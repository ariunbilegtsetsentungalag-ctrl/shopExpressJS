<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Simple Shop</title>
    <link rel="stylesheet" href="/main.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body { 
            padding-top: 80px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .main-navbar { 
            position: fixed; 
            top: 0; 
            z-index: 1001; 
            backdrop-filter: blur(10px);
            background: rgba(52, 58, 64, 0.95) !important;
        }
        .analytics-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            margin: 20px;
            overflow: hidden;
        }
        .analytics-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        .analytics-header h1 {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 10px;
        }
        .stat-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 15px;
            padding: 25px;
            text-align: center;
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
            margin-bottom: 20px;
            transition: transform 0.3s ease;
        }
        .stat-card:hover {
            transform: translateY(-5px);
        }
        .stat-card.spending {
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
        }
        .stat-card.orders {
            background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
        }
        .stat-card.average {
            background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
        }
        .stat-card.monthly {
            background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
            color: #333;
        }
        .stat-card h3 {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 10px;
        }
        .stat-card p {
            margin: 0;
            font-size: 1.1rem;
            opacity: 0.9;
        }
        .chart-container {
            position: relative;
            height: 400px;
            margin: 20px 0;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 15px;
        }
        .section-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            padding: 25px;
            margin-bottom: 25px;
        }
        .section-title {
            color: #495057;
            font-weight: bold;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .recent-order {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 15px;
            margin-bottom: 8px;
            background: #f8f9fa;
            border-radius: 8px;
            border-left: 4px solid #007bff;
        }
        .order-date {
            font-weight: 500;
            color: #495057;
        }
        .order-amount {
            font-weight: bold;
            color: #28a745;
        }
        .empty-state {
            text-align: center;
            padding: 40px;
            color: #6c757d;
        }
        .empty-state i {
            font-size: 4rem;
            margin-bottom: 20px;
            opacity: 0.5;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <%- include('../include/header') %>

    <div class="analytics-container">
        <!-- Header -->
        <div class="analytics-header">
            <h1><i class="fas fa-chart-pie"></i> My Spending Analytics</h1>
            <p>Welcome back, <%= username %>! Track your purchase history and spending patterns</p>
        </div>

        <div class="container-fluid p-4">
            <!-- Statistics Cards -->
            <div class="row mb-4">
                <div class="col-md-3">
                    <div class="stat-card spending">
                        <div class="stat-icon mb-3">
                            <i class="fas fa-wallet fa-2x"></i>
                        </div>
                        <h3>$<%= totalSpent.toFixed(2) %></h3>
                        <p>Total Spent</p>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="stat-card orders">
                        <div class="stat-icon mb-3">
                            <i class="fas fa-shopping-bag fa-2x"></i>
                        </div>
                        <h3><%= totalOrders %></h3>
                        <p>Total Orders</p>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="stat-card average">
                        <div class="stat-icon mb-3">
                            <i class="fas fa-calculator fa-2x"></i>
                        </div>
                        <h3>$<%= averageOrderValue.toFixed(2) %></h3>
                        <p>Average Order</p>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="stat-card monthly">
                        <div class="stat-icon mb-3">
                            <i class="fas fa-calendar-month fa-2x"></i>
                        </div>
                        <h3>$<%= thisMonthSpent.toFixed(2) %></h3>
                        <p>This Month</p>
                    </div>
                </div>
            </div>

            <div class="row">
                <!-- Monthly Spending Chart -->
                <div class="col-md-8">
                    <div class="section-card">
                        <h5 class="section-title">
                            <i class="fas fa-chart-bar"></i> Monthly Spending (This Year)
                        </h5>
                        <% if (monthlySpending.length > 0) { %>
                        <div class="chart-container">
                            <canvas id="monthlySpendingChart"></canvas>
                        </div>
                        <% } else { %>
                        <div class="alert alert-info">
                            <i class="fas fa-info-circle"></i>
                            <strong>No spending data yet.</strong> Your monthly spending chart will appear here once you make purchases.
                            <a href="/shop" class="btn btn-sm btn-primary ms-2">
                                <i class="fas fa-shopping-cart"></i> Start Shopping
                            </a>
                        </div>
                        <% } %>
                    </div>
                </div>

                <!-- Recent Orders -->
                <div class="col-md-4">
                    <div class="section-card">
                        <h5 class="section-title">
                            <i class="fas fa-history"></i> Recent Orders
                        </h5>
                        <% if (recentOrders.length > 0) { %>
                            <% recentOrders.forEach(order => { %>
                                <div class="recent-order">
                                    <div>
                                        <div class="order-date">
                                            <%= new Date(order.orderDate).toLocaleDateString() %>
                                        </div>
                                        <small class="text-muted">
                                            <%= order.products ? order.products.length : 0 %> items
                                        </small>
                                    </div>
                                    <div class="order-amount">
                                        $<%= order.totalAmount.toFixed(2) %>
                                    </div>
                                </div>
                            <% }) %>
                            <div class="text-center mt-3">
                                <a href="/order-history" class="btn btn-outline-primary">
                                    <i class="fas fa-list"></i> View All Orders
                                </a>
                            </div>
                        <% } else { %>
                            <div class="empty-state">
                                <i class="fas fa-shopping-cart"></i>
                                <h6>No orders yet</h6>
                                <p>Your order history will appear here</p>
                                <a href="/shop" class="btn btn-primary mt-3">
                                    <i class="fas fa-shopping-cart"></i> Start Shopping
                                </a>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>

            <!-- Summary Section -->
            <div class="row">
                <div class="col-12">
                    <div class="section-card">
                        <h5 class="section-title">
                            <i class="fas fa-chart-line"></i> Spending Summary
                        </h5>
                        <div class="row">
                            <div class="col-md-4 text-center">
                                <h4 class="text-primary">$<%= yearlySpendingTotal.toFixed(2) %></h4>
                                <p class="text-muted">Total This Year</p>
                            </div>
                            <div class="col-md-4 text-center">
                                <h4 class="text-success"><%= thisMonthOrders %></h4>
                                <p class="text-muted">Orders This Month</p>
                            </div>
                            <div class="col-md-4 text-center">
                                <h4 class="text-info">
                                    <% if (totalOrders > 0) { %>
                                        <%= Math.round((thisMonthSpent / totalSpent) * 100) %>%
                                    <% } else { %>
                                        0%
                                    <% } %>
                                </h4>
                                <p class="text-muted">Of Total Spending</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Call to Action -->
            <div class="row mt-4">
                <div class="col-12">
                    <div class="text-center p-4 bg-primary text-white rounded">
                        <h5><i class="fas fa-shopping-cart"></i> Ready for more shopping?</h5>
                        <p class="mb-3">Discover new products and exclusive deals</p>
                        <a href="/shop" class="btn btn-light btn-lg">
                            <i class="fas fa-store"></i> Browse Products
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            <% if (monthlySpending.length > 0) { %>
            // Monthly Spending Chart
            const ctx = document.getElementById('monthlySpendingChart').getContext('2d');
            const monthNames = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
            const monthlyData = new Array(12).fill(0);
            
            <% monthlySpending.forEach(item => { %>
            monthlyData[<%= item._id - 1 %>] = <%= item.totalSpent %>;
            <% }); %>
            
            new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: monthNames,
                    datasets: [{
                        label: 'Monthly Spending ($)',
                        data: monthlyData,
                        backgroundColor: 'rgba(102, 126, 234, 0.8)',
                        borderColor: 'rgba(102, 126, 234, 1)',
                        borderWidth: 2,
                        borderRadius: 5,
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            display: true,
                            position: 'top'
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            ticks: {
                                callback: function(value) {
                                    return '$' + value.toFixed(2);
                                }
                            }
                        }
                    }
                }
            });
            <% } %>
        });
    </script>
</body>
</html>